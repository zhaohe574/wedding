# 设计文档：UniApp前端UI重构

## 概述

本设计文档描述了婚庆服务预约管理系统UniApp前端的UI重构方案。重构将基于《UI设计规划.md》中定义的设计系统，采用浪漫优雅的紫色调主题，使用图鸟UI组件库，确保在小程序、H5和App平台上提供一致且高质量的用户体验。

### 设计目标

1. **视觉一致性**：建立统一的设计语言，包括颜色、字体、间距、组件样式
2. **品牌特色**：体现婚庆行业的浪漫优雅气质
3. **用户体验**：提供流畅的交互和清晰的信息层级
4. **可维护性**：通过设计令牌和组件化提高代码可维护性
5. **可配置性**：支持通过装修系统灵活配置页面

### 技术栈

- **框架**：UniApp (Vue 3 + TypeScript)
- **UI库**：图鸟UI (@tuniao/tnui-vue3-uniapp)
- **样式**：Tailwind CSS + SCSS
- **状态管理**：Pinia
- **平台**：微信小程序、H5、Android/iOS App

## 架构设计

### 整体架构

```
┌─────────────────────────────────────────┐
│           应用层 (App.vue)               │
│  - 主题初始化                            │
│  - 全局配置加载                          │
└─────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼────────┐    ┌────────▼────────┐
│   页面层        │    │   组件层         │
│  - 首页         │    │  - 基础组件      │
│  - 详情页       │    │  - 业务组件      │
│  - 列表页       │    │  - 装修组件      │
└───────┬────────┘    └────────┬────────┘
        │                      │
        └──────────┬───────────┘
                   │
        ┌──────────▼──────────┐
        │     设计系统层       │
        │  - 主题配置          │
        │  - 设计令牌          │
        │  - 样式工具          │
        └─────────────────────┘
```

### 设计系统层

设计系统层是整个UI重构的基础，包含以下核心模块：


#### 1. 主题配置模块 (Theme Store)

**职责**：管理全局主题配置，包括颜色、导航栏样式等

**接口**：
```typescript
interface ThemeStore {
  // 主题色
  primaryColor: string        // 主色 #7C3AED
  secondaryColor: string      // 辅助色 #EC4899
  ctaColor: string           // CTA色 #F97316
  accentColor: string        // 点缀色 #FFD700
  
  // 导航栏
  navColor: string           // 导航栏文字色
  navBgColor: string         // 导航栏背景色
  
  // CSS变量字符串
  vars: string
  
  // 方法
  getTheme(): Promise<void>  // 从装修系统获取主题
  setTheme(color: string): void  // 设置主题色
  generateVars(): string     // 生成CSS变量
}
```

**实现要点**：
- 从装修系统API（id=5）获取主题配置
- 使用`generateVars`工具函数生成CSS变量
- 支持动态切换主题色
- 自动生成5个亮度变体（light-3, light-5, light-7, light-9, dark-2）

#### 2. 设计令牌 (Design Tokens)

设计令牌是设计系统的原子单位，定义在Tailwind配置和CSS变量中。

**颜色令牌**：
```javascript
// tailwind.config.js
colors: {
  // 主题色
  primary: {
    DEFAULT: 'var(--color-primary)',
    'light-3': 'var(--color-primary-light-3)',
    'light-5': 'var(--color-primary-light-5)',
    'light-7': 'var(--color-primary-light-7)',
    'light-9': 'var(--color-primary-light-9)',
    'dark-2': 'var(--color-primary-dark-2)'
  },
  // 功能色
  success: { /* 同上结构 */ },
  warning: { /* 同上结构 */ },
  error: { /* 同上结构 */ },
  info: { /* 同上结构 */ },
  // 中性色
  white: '#FFFFFF',
  black: '#000000',
  main: '#333333',
  content: '#666666',
  muted: '#999999',
  page: '#F6F6F6',
  light: '#E5E5E5',
  disabled: '#C8C9CC'
}
```

**字号令牌**：
```javascript
fontSize: {
  xs: '24rpx',    // 说明文本
  sm: '26rpx',    // 辅助文本
  base: '28rpx',  // 正文
  lg: '30rpx',
  xl: '32rpx',    // 三级标题
  '2xl': '34rpx', // 二级标题
  '3xl': '38rpx',
  '4xl': '40rpx', // 一级标题
  '5xl': '44rpx'  // 大标题
}
```

**间距令牌**：
```javascript
spacing: {
  xs: '8rpx',
  sm: '16rpx',
  md: '24rpx',
  lg: '32rpx',
  xl: '48rpx',
  '2xl': '64rpx'
}
```

#### 3. 主题工具函数

**generateVarsMap**：为单个颜色生成变体映射
```typescript
function generateVarsMap(
  color: string,
  type: string = 'primary',
  isDark: boolean = false
): Record<string, string>
```

**generateVars**：生成完整的CSS变量字符串
```typescript
function generateVars(
  options: Record<string, string>,
  extra: Record<string, string> = {},
  isDark: boolean = false
): string
```

**使用示例**：
```typescript
const vars = generateVars(
  { primary: '#7C3AED' },
  { 
    '--color-secondary': '#EC4899',
    '--color-cta': '#F97316',
    '--color-accent': '#FFD700'
  }
)
// 输出：--color-primary:#7C3AED;--color-primary-light-3:...
```


## 组件设计

### 基础组件

#### 1. 按钮组件 (Button)

**组件规格**：
```vue
<tn-button
  :type="'primary' | 'secondary' | 'cta'"
  :size="'lg' | 'md' | 'sm'"
  :shape="'round'"
  @click="handleClick"
>
  按钮文字
</tn-button>
```

**样式规范**：

| 类型 | 背景色 | 文字色 | 边框 | 使用场景 |
|------|--------|--------|------|---------|
| primary | #7C3AED | white | 无 | 主要操作 |
| secondary | transparent | #7C3AED | 2rpx #7C3AED | 次要操作 |
| cta | #F97316 | white | 无 | 关键行动 |

| 尺寸 | 高度 | 内边距 | 字号 |
|------|------|--------|------|
| lg | 88rpx | 32rpx | 32rpx |
| md | 72rpx | 24rpx | 28rpx |
| sm | 56rpx | 16rpx | 26rpx |

**交互效果**：
- 圆角：48rpx
- 过渡：transition: all 0.2s ease
- 点击：transform: scale(0.98); opacity: 0.9
- 禁用：opacity: 0.6; cursor: not-allowed

#### 2. 卡片组件 (Card)

**标准卡片**：
```vue
<view class="card-standard">
  <slot />
</view>
```

**样式**：
```scss
.card-standard {
  background: #FFFFFF;
  border-radius: 16rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
  
  &:active {
    box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.12);
    transform: translateY(-4rpx);
  }
}
```

**玻璃态卡片**：
```vue
<view class="card-glass">
  <slot />
</view>
```

**样式**：
```scss
.card-glass {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 20rpx;
  padding: 24rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.1);
}
```

**使用场景**：
- 标准卡片：列表项、信息展示
- 玻璃态卡片：用户信息、动态卡片、特色展示

#### 3. 导航栏组件 (Navbar)

**组件规格**：
```vue
<tn-navbar
  :back-icon="true"
  :fixed="true"
  :bg-color="navBgColor"
  :text-color="navColor"
  :title="pageTitle"
/>
```

**配置**：
- 高度：88rpx（含状态栏）
- 背景：从主题配置获取
- 文字色：白色或黑色（根据背景自适应）
- 返回按钮：左侧，使用图鸟UI图标

**使用方式**：
```vue
<script setup>
import { useThemeStore } from '@/stores/theme'
const themeStore = useThemeStore()
</script>

<template>
  <tn-navbar
    :back-icon="true"
    :fixed="true"
    :bg-color="themeStore.navBgColor"
    :text-color="themeStore.navColor"
    title="页面标题"
  />
</template>
```

#### 4. 表单组件

**输入框 (Input)**：
```vue
<tn-input
  v-model="value"
  :placeholder="'请输入'"
  :height="88"
  :border-radius="12"
/>
```

**样式规范**：
- 高度：88rpx
- 内边距：24rpx
- 背景：#F8F8F8（默认）、#FFFFFF（焦点）
- 边框：2rpx transparent（默认）、2rpx primary（焦点）
- 圆角：12rpx
- 过渡：transition: all 0.2s ease

**选择器 (Picker)**：
```vue
<tn-picker
  v-model="selected"
  :options="options"
/>
```

**日期选择器 (DatePicker)**：
```vue
<tn-date-picker
  v-model="date"
  :format="'YYYY-MM-DD'"
/>
```


### 业务组件

#### 1. 底部标签栏 (Tabbar)

**组件结构**：
```vue
<template>
  <view class="custom-tabbar">
    <view
      v-for="(item, index) in tabbarList"
      :key="index"
      class="tabbar-item"
      :class="{ active: currentIndex === index }"
      @click="switchTab(item, index)"
    >
      <tn-icon
        :name="currentIndex === index ? item.selectedIcon : item.icon"
        :size="48"
        :color="currentIndex === index ? primaryColor : '#999999'"
      />
      <text class="tabbar-text">{{ item.text }}</text>
    </view>
  </view>
</template>
```

**样式规范**：
- 高度：120rpx + 安全区域
- 背景：#FFFFFF
- 图标尺寸：48rpx × 48rpx
- 选中状态：主色图标 + 主色文字
- 未选中：灰色图标 + 灰色文字

**图标映射**：
```typescript
const tabbarList = [
  {
    text: '首页',
    icon: 'home',
    selectedIcon: 'home-fill',
    pagePath: '/pages/index/index'
  },
  {
    text: '资讯',
    icon: 'news',
    selectedIcon: 'news-fill',
    pagePath: '/pages/news/news'
  },
  {
    text: '我的',
    icon: 'user',
    selectedIcon: 'user-fill',
    pagePath: '/pages/user/user'
  }
]
```

#### 2. 人员卡片 (StaffCard)

**组件规格**：
```vue
<staff-card
  :staff="staffData"
  :show-favorite="true"
  @click="goToDetail"
  @favorite="handleFavorite"
/>
```

**数据结构**：
```typescript
interface StaffData {
  id: number
  name: string
  avatar: string
  category: string
  rating: number
  reviewCount: number
  price: number
  tags: string[]
  isFavorite: boolean
}
```

**布局结构**：
```
┌─────────────────────────────────┐
│  ┌────┐  姓名 ★4.9 (128)        │
│  │头像│  服务类型                │
│  └────┘  ¥1280起                │
│  [标签1] [标签2] [标签3]    ♥   │
└─────────────────────────────────┘
```

**样式要点**：
- 使用标准卡片样式
- 头像：圆形，80rpx × 80rpx
- 评分：星级图标 + 数字 + 评价数
- 价格：橙色（CTA色）
- 标签：浅紫背景 + 紫色文字
- 收藏按钮：右上角，心形图标

#### 3. 动态卡片 (DynamicCard)

**组件规格**：
```vue
<dynamic-card
  :dynamic="dynamicData"
  @like="handleLike"
  @comment="handleComment"
  @share="handleShare"
/>
```

**数据结构**：
```typescript
interface DynamicData {
  id: number
  user: {
    id: number
    nickname: string
    avatar: string
  }
  content: string
  images: string[]
  topics: string[]
  location?: string
  viewCount: number
  likeCount: number
  commentCount: number
  isLiked: boolean
  createTime: string
}
```

**布局结构**：
```
┌─────────────────────────────────┐
│  ┌──┐ 用户昵称        [关注]    │
│  │头│ 2小时前                   │
│  └──┘                           │
│  文字内容 #话题标签              │
│  ┌───┬───┬───┐                 │
│  │图1│图2│图3│                 │
│  ├───┼───┼───┤                 │
│  │图4│图5│图6│                 │
│  └───┴───┴───┘                 │
│  📍 位置信息                     │
│  👁 1.2k  ❤ 89  💬 23  ↗       │
└─────────────────────────────────┘
```

**样式要点**：
- 使用标准卡片样式
- 头像：圆形，64rpx × 64rpx
- 关注按钮：紫色背景，圆角
- 话题标签：紫色文字，可点击
- 图片：九宫格布局，间距8rpx
- 互动栏：图标 + 数字，灰色

#### 4. 订单卡片 (OrderCard)

**组件规格**：
```vue
<order-card
  :order="orderData"
  @action="handleAction"
/>
```

**数据结构**：
```typescript
interface OrderData {
  id: number
  orderNo: string
  status: 'pending' | 'unpaid' | 'paid' | 'completed' | 'cancelled'
  staff: {
    name: string
    avatar: string
    category: string
  }
  serviceDate: string
  location: string
  originalPrice: number
  discount: number
  actualPrice: number
  actions: Array<{
    text: string
    type: 'primary' | 'secondary'
    action: string
  }>
}
```

**状态色彩**：
```typescript
const statusColors = {
  pending: '#FF9900',    // 待确认
  unpaid: '#F97316',     // 待支付
  paid: '#10B981',       // 已支付
  completed: '#6B7280',  // 已完成
  cancelled: '#EF4444'   // 已取消
}
```

**布局结构**：
```
┌─────────────────────────────────┐
│  [待支付]                        │
│  ┌──┐ 人员姓名                   │
│  │头│ 服务类型                   │
│  └──┘                           │
│  服务日期：2024-02-14 14:00     │
│  服务地点：XX酒店               │
│  ────────────────────────────   │
│  原价：¥1500                    │
│  优惠：-¥200                    │
│  实付：¥1300                    │
│  [取消订单]        [立即支付]   │
└─────────────────────────────────┘
```


### 装修组件

装修组件是可通过后台装修系统配置的页面组件，支持内容和样式的动态配置。

#### 1. 搜索栏组件 (w-search)

**Props接口**：
```typescript
interface SearchProps {
  content: {
    placeholder: string
    hotWords: string[]
  }
  styles: {
    bgColor: string
    textColor: string
    borderRadius: number
  }
}
```

**样式规范**：
- 高度：72rpx
- 圆角：36rpx（全圆角）
- 背景：白色或自定义
- 图标：搜索图标，左侧
- 热词：可点击，紫色文字

#### 2. 轮播图组件 (w-banner)

**Props接口**：
```typescript
interface BannerProps {
  content: {
    list: Array<{
      image: string
      link: string
      title?: string
    }>
    autoplay: boolean
    interval: number
    indicatorDots: boolean
  }
  styles: {
    height: number
    borderRadius: number
    margin: number
  }
}
```

**样式规范**：
- 默认高度：320rpx
- 圆角：16rpx
- 指示器：圆点，紫色
- 间距：24rpx（左右）

#### 3. 导航菜单组件 (w-nav)

**Props接口**：
```typescript
interface NavProps {
  content: {
    list: Array<{
      icon: string
      text: string
      link: string
      badge?: number
    }>
    columns: 4 | 5
  }
  styles: {
    iconSize: number
    textSize: number
    bgColor: string
  }
}
```

**布局**：
- 网格布局：4列或5列
- 图标尺寸：64rpx × 64rpx
- 文字：26rpx，居中
- 间距：24rpx

#### 4. 人员推荐组件 (w-staff-showcase)

**Props接口**：
```typescript
interface StaffShowcaseProps {
  content: {
    title: string
    list: StaffData[]
    showMore: boolean
  }
  styles: {
    titleSize: number
    cardStyle: 'horizontal' | 'vertical'
  }
}
```

**布局**：
- 标题：32rpx，字重600
- 横向滚动：scroll-view
- 卡片间距：16rpx
- 查看更多：右侧箭头

#### 5. 服务套餐组件 (w-service-packages)

**Props接口**：
```typescript
interface ServicePackagesProps {
  content: {
    title: string
    list: Array<{
      id: number
      name: string
      image: string
      price: number
      originalPrice: number
      description: string
    }>
  }
  styles: {
    columns: 2 | 3
    cardRadius: number
  }
}
```

**布局**：
- 网格布局：2列或3列
- 图片比例：16:9
- 价格：橙色，加粗
- 原价：删除线，灰色


## 页面设计

### 1. 首页 (pages/index/index)

**页面结构**：
```
┌─────────────────────────────────┐
│  [搜索栏]                        │
│  ┌─────────────────────────┐   │
│  │     轮播图               │   │
│  └─────────────────────────┘   │
│  [导航菜单 - 4列]               │
│  ┌─────────────────────────┐   │
│  │  人员推荐 →              │   │
│  │  [卡片1][卡片2][卡片3]   │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  服务套餐                │   │
│  │  [套餐1] [套餐2]         │   │
│  │  [套餐3] [套餐4]         │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  案例作品                │   │
│  │  [作品1] [作品2]         │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  客户评价 →              │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

**样式配置**：
- 背景：浅紫色渐变（#FAF5FF → #FFFFFF）
- 区块间距：32rpx
- 内容边距：24rpx

**装修配置**：
```typescript
interface IndexPageConfig {
  meta: {
    title: string
    bg_type: 1 | 2
    bg_color: string
    bg_image: string
  }
  data: Array<{
    name: 'search' | 'banner' | 'nav' | 'staff-showcase' | 'service-packages' | 'portfolio-gallery' | 'customer-reviews'
    content: any
    styles: any
  }>
}
```

### 2. 人员详情页 (packages/pages/staff_detail/staff_detail)

**页面结构**：
```
┌─────────────────────────────────┐
│  ┌─────────────────────────┐   │
│  │     头图（玻璃态导航）    │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  ┌──┐ 姓名 ★4.9         │   │
│  │  │头│ 服务类型           │   │
│  │  └──┘ ¥1280起           │   │
│  │  ♥ 收藏  ↗ 分享         │   │
│  └─────────────────────────┘   │
│  [简介] [作品] [评价]           │
│  ┌─────────────────────────┐   │
│  │  内容区域                │   │
│  │                         │   │
│  └─────────────────────────┘   │
│  ────────────────────────────   │
│  [查看档期]      [立即预约]     │
└─────────────────────────────────┘
```

**关键设计**：
- 头图：高度400rpx，玻璃态导航栏
- 信息卡片：玻璃态效果，悬浮在头图上
- 标签页：固定在信息卡片下方
- 作品展示：九宫格，间距8rpx
- 底部操作栏：固定，高度120rpx + 安全区域

**交互**：
- 收藏：点击切换状态，动画效果
- 分享：打开分享面板
- 标签页：滑动切换内容
- 作品：点击查看大图

### 3. 动态广场页 (pages/dynamic/dynamic)

**页面结构**：
```
┌─────────────────────────────────┐
│  [关注] [推荐] [话题]            │
│  [最新 ▼] [筛选]                │
│  ┌─────────────────────────┐   │
│  │  动态卡片1               │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  动态卡片2               │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  动态卡片3               │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

**样式配置**：
- 标签栏：固定顶部，白色背景
- 选中标签：紫色文字，底部紫色线条
- 筛选按钮：右侧，图标 + 文字
- 卡片间距：16rpx

**交互**：
- 下拉刷新：加载最新动态
- 上拉加载：加载更多
- 点赞：动画效果，数字增加
- 评论：跳转详情页
- 关注：切换状态

### 4. 订单页面 (pages/order/order)

**页面结构**：
```
┌─────────────────────────────────┐
│  [全部] [待支付] [已支付] [已完成]│
│  ┌─────────────────────────┐   │
│  │  订单卡片1               │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  订单卡片2               │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  订单卡片3               │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

**样式配置**：
- 标签栏：固定顶部，白色背景
- 选中标签：紫色文字，底部紫色线条
- 卡片间距：16rpx
- 空状态：居中显示，图标 + 文字

**交互**：
- 切换标签：筛选订单
- 下拉刷新：更新订单列表
- 点击卡片：查看订单详情
- 操作按钮：根据状态显示不同按钮

### 5. 购物车页面 (packages/pages/cart/cart)

**页面结构**：
```
┌─────────────────────────────────┐
│  ☑ 全选    [管理]               │
│  ┌─────────────────────────┐   │
│  │ ☑ 项目1  ¥1280          │   │
│  │   2024-02-14 14:00      │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │ ☑ 项目2  ¥980  ⚠冲突   │   │
│  │   2024-02-14 15:00      │   │
│  └─────────────────────────┘   │
│  ────────────────────────────   │
│  合计：¥2260                    │
│  [保存方案]      [去结算]       │
└─────────────────────────────────┘
```

**样式配置**：
- 选中状态：紫色边框 + 浅紫背景
- 冲突提示：红色图标 + 红色文字
- 费用明细：固定底部，玻璃态背景
- 底部高度：160rpx + 安全区域

**交互**：
- 全选：切换所有项目选中状态
- 单选：切换单个项目选中状态
- 管理模式：显示删除按钮
- 冲突检测：实时检查档期冲突
- 保存方案：保存当前选择
- 去结算：跳转结算页面


## 数据模型

### 主题配置模型

```typescript
interface ThemeConfig {
  // 从装修系统获取的原始配置
  themeColor1: string        // 主色
  themeColor2: string        // 辅助色
  buttonColor: string        // 按钮文字色
  navigationBarColor: string // 导航栏背景色
  topTextColor: 'white' | 'black' // 导航栏文字色
}

interface ThemeState {
  // Store中的状态
  primaryColor: string       // 主色
  secondaryColor: string     // 辅助色
  ctaColor: string          // CTA色
  accentColor: string       // 点缀色
  btnColor: string          // 按钮文字色
  navColor: string          // 导航栏文字色
  navBgColor: string        // 导航栏背景色
  vars: string              // CSS变量字符串
}
```

### 装修配置模型

```typescript
interface DecorateConfig {
  id: number
  name: string
  type: number  // 1:首页 2:个人中心 5:系统风格
  data: string | {
    meta: {
      title: string
      bg_type: 1 | 2
      bg_color: string
      bg_image: string
    }
    data: ComponentConfig[]
  }
}

interface ComponentConfig {
  id: string
  name: string
  title: string
  content: Record<string, any>
  styles: Record<string, any>
}
```

### 组件数据模型

```typescript
// 人员数据
interface StaffData {
  id: number
  name: string
  avatar: string
  category: string
  categoryId: number
  rating: number
  reviewCount: number
  price: number
  originalPrice: number
  tags: string[]
  isFavorite: boolean
  description: string
  images: string[]
  services: string[]
}

// 动态数据
interface DynamicData {
  id: number
  user: {
    id: number
    nickname: string
    avatar: string
    isFollowed: boolean
  }
  content: string
  images: string[]
  topics: Array<{
    id: number
    name: string
  }>
  location?: {
    name: string
    lat: number
    lng: number
  }
  viewCount: number
  likeCount: number
  commentCount: number
  isLiked: boolean
  createTime: string
}

// 订单数据
interface OrderData {
  id: number
  orderNo: string
  status: 'pending' | 'unpaid' | 'paid' | 'completed' | 'cancelled'
  staff: {
    id: number
    name: string
    avatar: string
    category: string
  }
  serviceDate: string
  serviceTime: string
  location: string
  originalPrice: number
  discount: number
  actualPrice: number
  createTime: string
  payTime?: string
  completeTime?: string
  actions: OrderAction[]
}

interface OrderAction {
  text: string
  type: 'primary' | 'secondary' | 'danger'
  action: 'pay' | 'cancel' | 'confirm' | 'review' | 'detail'
}

// 购物车数据
interface CartItem {
  id: number
  staffId: number
  staffName: string
  staffAvatar: string
  category: string
  serviceDate: string
  serviceTime: string
  price: number
  isSelected: boolean
  hasConflict: boolean
  conflictReason?: string
}

interface CartPlan {
  id: number
  name: string
  items: CartItem[]
  totalPrice: number
  createTime: string
}
```


## 正确性属性

*属性是一个特征或行为，应该在系统的所有有效执行中保持为真——本质上是关于系统应该做什么的正式陈述。属性作为人类可读规范和机器可验证正确性保证之间的桥梁。*

### 属性1：主题色变体生成完整性

*对于任意*有效的主题色值，当调用generateVarsMap函数时，应该生成包含5个亮度变体（light-3, light-5, light-7, light-9, dark-2）的完整映射对象。

**验证：需求 1.2**

### 属性2：CSS变量全局可配置性

*对于任意*主题色配置，当通过ThemeStore更新主题色时，所有使用该主题色的组件应该立即反映新的颜色值。

**验证：需求 1.5**

### 属性3：装修系统主题色同步

*对于任意*装修系统的主题色配置更新，当调用getTheme方法后，ThemeStore中的颜色值应该与装修配置中的值保持一致。

**验证：需求 1.7**

### 属性4：间距系统8rpx基准

*对于任意*定义在间距系统中的间距值，该值应该是8rpx的整数倍。

**验证：需求 3.1**

### 属性5：按钮类型样式映射

*对于任意*按钮类型（primary、secondary、cta），渲染后的按钮应该具有该类型对应的正确背景色、文字色和边框样式。

**验证：需求 4.2, 4.3, 4.4**

### 属性6：按钮点击视觉反馈

*对于任意*可点击的按钮，当用户触发点击事件时，按钮应该显示缩放（scale 0.98）和透明度降低（opacity 0.9）的视觉反馈。

**验证：需求 4.7**

### 属性7：卡片类型样式映射

*对于任意*卡片类型（standard、glass），渲染后的卡片应该具有该类型对应的正确背景、边框、阴影和模糊效果。

**验证：需求 5.2, 5.3, 5.4, 5.5, 5.6**

### 属性8：玻璃态卡片对比度

*对于任意*玻璃态卡片中的文字，文字与背景的对比度应该达到或超过4.5:1。

**验证：需求 5.7**

### 属性9：表单输入框焦点状态

*对于任意*输入框，当获得焦点时，背景色应该从#F8F8F8变为#FFFFFF，边框色应该从transparent变为主色。

**验证：需求 8.4**

### 属性10：图标系统一致性

*对于任意*使用图标的组件，应该使用tn-icon组件而不是Emoji或图片。

**验证：需求 9.2, 9.6**

### 属性11：装修配置实时应用

*对于任意*装修配置的更新，当配置保存后，前端页面应该立即反映新的样式和布局，无需刷新页面。

**验证：需求 15.8**

### 属性12：响应式布局断点

*对于任意*视口宽度，页面布局应该根据断点规则正确显示：< 768px单列，768px-1024px双列，> 1024px三列或更多。

**验证：需求 16.1, 16.2, 16.3**

### 属性13：过渡动画时长规范

*对于任意*使用过渡动画的元素，过渡时长应该在150ms-300ms范围内，不应超过500ms。

**验证：需求 17.1, 17.2, 17.3, 17.7**

### 属性14：文字对比度达标

*对于任意*文字元素，正文文字与背景的对比度应该达到或超过4.5:1，大号文字应该达到或超过3:1。

**验证：需求 18.1, 18.2**

### 属性15：触摸目标尺寸达标

*对于任意*可交互元素，触摸目标的尺寸应该不小于88rpx × 88rpx。

**验证：需求 18.4**

### 属性16：图片懒加载启用

*对于任意*图片元素，应该启用lazy-load属性以实现懒加载。

**验证：需求 19.1**


## 错误处理

### 主题加载失败

**场景**：从装修系统API获取主题配置失败

**处理策略**：
1. 使用默认主题配置（紫色调方案）
2. 记录错误日志到控制台
3. 显示toast提示用户"主题加载失败，使用默认主题"
4. 不阻塞页面渲染

**默认配置**：
```typescript
const defaultTheme: ThemeConfig = {
  themeColor1: '#7C3AED',
  themeColor2: '#EC4899',
  buttonColor: 'white',
  navigationBarColor: '#7C3AED',
  topTextColor: 'white'
}
```

### 装修配置解析失败

**场景**：装修配置数据格式错误或JSON解析失败

**处理策略**：
1. 捕获解析错误
2. 记录错误详情到控制台
3. 使用空配置或默认配置
4. 显示错误提示
5. 允许页面继续渲染基础内容

**错误边界**：
```typescript
try {
  const config = JSON.parse(decorateData)
  return config
} catch (error) {
  console.error('装修配置解析失败:', error)
  uni.showToast({
    title: '配置加载失败',
    icon: 'none'
  })
  return getDefaultConfig()
}
```

### 图片加载失败

**场景**：图片URL无效或网络错误导致加载失败

**处理策略**：
1. 使用@error事件监听加载失败
2. 显示占位图片
3. 提供重试机制（点击重新加载）
4. 不影响其他内容显示

**实现**：
```vue
<image
  :src="imageUrl"
  @error="handleImageError"
  mode="aspectFill"
/>

<script setup>
const handleImageError = (e) => {
  e.target.src = '/static/images/placeholder.png'
}
</script>
```

### API请求失败

**场景**：网络请求超时或服务器错误

**处理策略**：
1. 显示友好的错误提示
2. 提供重试按钮
3. 对于关键数据，使用缓存降级
4. 记录错误信息用于分析

**错误提示**：
- 网络错误：显示"网络连接失败，请检查网络设置"
- 超时：显示"请求超时，请稍后重试"
- 服务器错误：显示"服务暂时不可用，请稍后重试"

### 档期冲突

**场景**：购物车中存在时间冲突的预约项目

**处理策略**：
1. 实时检测档期冲突
2. 在冲突项目上显示红色警告图标
3. 显示冲突原因（如"与项目X时间冲突"）
4. 阻止提交订单，要求用户解决冲突
5. 提供快速解决方案（如移除冲突项目）

**冲突检测算法**：
```typescript
function detectConflict(items: CartItem[]): CartItem[] {
  return items.map(item => {
    const conflicts = items.filter(other => 
      other.id !== item.id &&
      isTimeOverlap(item.serviceDate, item.serviceTime, other.serviceDate, other.serviceTime)
    )
    return {
      ...item,
      hasConflict: conflicts.length > 0,
      conflictReason: conflicts.length > 0 
        ? `与${conflicts[0].staffName}的预约时间冲突` 
        : undefined
    }
  })
}
```

### 支付失败

**场景**：支付过程中出现错误

**处理策略**：
1. 捕获支付错误
2. 区分错误类型（用户取消、余额不足、系统错误等）
3. 显示对应的错误提示
4. 保留订单状态，允许重新支付
5. 提供客服联系方式

**错误分类**：
- 用户取消：不显示错误，返回订单页
- 余额不足：提示充值
- 支付超时：提示重试
- 系统错误：提示联系客服


## 测试策略

### 双重测试方法

本项目采用单元测试和属性测试相结合的方法，确保全面的测试覆盖：

- **单元测试**：验证特定示例、边缘情况和错误条件
- **属性测试**：验证跨所有输入的通用属性
- 两者互补，共同提供全面覆盖（单元测试捕获具体错误，属性测试验证通用正确性）

### 单元测试

单元测试专注于特定示例和边缘情况，避免过度测试。

#### 主题系统测试

**测试文件**：`tests/unit/theme.spec.ts`

**测试用例**：
```typescript
describe('主题系统', () => {
  test('应该正确解析装修配置', () => {
    const config = {
      themeColor1: '#7C3AED',
      themeColor2: '#EC4899',
      buttonColor: 'white',
      navigationBarColor: '#7C3AED',
      topTextColor: 'white'
    }
    const theme = parseThemeConfig(config)
    expect(theme.primaryColor).toBe('#7C3AED')
    expect(theme.secondaryColor).toBe('#EC4899')
  })

  test('应该处理JSON字符串格式的配置', () => {
    const configStr = JSON.stringify({
      themeColor1: '#7C3AED'
    })
    const theme = parseThemeConfig(configStr)
    expect(theme.primaryColor).toBe('#7C3AED')
  })

  test('应该在解析失败时使用默认配置', () => {
    const invalidConfig = 'invalid json'
    const theme = parseThemeConfig(invalidConfig)
    expect(theme.primaryColor).toBe('#7C3AED') // 默认值
  })
})
```

#### 组件样式测试

**测试文件**：`tests/unit/components/button.spec.ts`

**测试用例**：
```typescript
describe('按钮组件', () => {
  test('primary按钮应该有正确的样式', () => {
    const wrapper = mount(TnButton, {
      props: { type: 'primary' }
    })
    expect(wrapper.classes()).toContain('tn-button--primary')
    // 验证样式属性
  })

  test('secondary按钮应该有边框', () => {
    const wrapper = mount(TnButton, {
      props: { type: 'secondary' }
    })
    // 验证边框样式
  })

  test('禁用按钮应该不可点击', () => {
    const onClick = vi.fn()
    const wrapper = mount(TnButton, {
      props: { disabled: true },
      attrs: { onClick }
    })
    wrapper.trigger('click')
    expect(onClick).not.toHaveBeenCalled()
  })
})
```

#### 工具函数测试

**测试文件**：`tests/unit/utils/theme.spec.ts`

**测试用例**：
```typescript
describe('主题工具函数', () => {
  test('generateVarsMap应该生成5个变体', () => {
    const vars = generateVarsMap('#7C3AED', 'primary')
    expect(vars).toHaveProperty('--color-primary')
    expect(vars).toHaveProperty('--color-primary-light-3')
    expect(vars).toHaveProperty('--color-primary-light-5')
    expect(vars).toHaveProperty('--color-primary-light-7')
    expect(vars).toHaveProperty('--color-primary-light-9')
    expect(vars).toHaveProperty('--color-primary-dark-2')
  })

  test('generateVars应该生成CSS变量字符串', () => {
    const vars = generateVars({ primary: '#7C3AED' })
    expect(vars).toContain('--color-primary:')
    expect(vars).toContain('--color-primary-light-3:')
  })
})
```

### 属性测试

属性测试验证跨所有输入的通用属性，使用快速检查库（如fast-check）。

#### 测试配置

**框架**：Vitest + fast-check
**最小迭代次数**：100次
**标签格式**：`Feature: uniapp-ui-redesign, Property {number}: {property_text}`

#### 属性测试用例

**测试文件**：`tests/property/theme.property.spec.ts`

```typescript
import fc from 'fast-check'

describe('主题系统属性测试', () => {
  test('属性1：主题色变体生成完整性', () => {
    // Feature: uniapp-ui-redesign, Property 1: 主题色变体生成完整性
    fc.assert(
      fc.property(
        fc.hexaString({ minLength: 6, maxLength: 6 }).map(s => `#${s}`),
        (color) => {
          const vars = generateVarsMap(color, 'primary')
          // 验证包含所有5个变体
          expect(vars).toHaveProperty('--color-primary-light-3')
          expect(vars).toHaveProperty('--color-primary-light-5')
          expect(vars).toHaveProperty('--color-primary-light-7')
          expect(vars).toHaveProperty('--color-primary-light-9')
          expect(vars).toHaveProperty('--color-primary-dark-2')
          return Object.keys(vars).length === 6 // 包括原色
        }
      ),
      { numRuns: 100 }
    )
  })

  test('属性4：间距系统8rpx基准', () => {
    // Feature: uniapp-ui-redesign, Property 4: 间距系统8rpx基准
    const spacingValues = [8, 16, 24, 32, 48, 64]
    spacingValues.forEach(value => {
      expect(value % 8).toBe(0)
    })
  })
})
```

**测试文件**：`tests/property/components.property.spec.ts`

```typescript
describe('组件属性测试', () => {
  test('属性5：按钮类型样式映射', () => {
    // Feature: uniapp-ui-redesign, Property 5: 按钮类型样式映射
    fc.assert(
      fc.property(
        fc.constantFrom('primary', 'secondary', 'cta'),
        (type) => {
          const wrapper = mount(TnButton, { props: { type } })
          const styles = getComputedStyle(wrapper.element)
          
          // 验证样式映射
          if (type === 'primary') {
            expect(styles.backgroundColor).toMatch(/#7C3AED/i)
            expect(styles.color).toMatch(/white/i)
          } else if (type === 'secondary') {
            expect(styles.backgroundColor).toMatch(/transparent/i)
            expect(styles.borderColor).toMatch(/#7C3AED/i)
          } else if (type === 'cta') {
            expect(styles.backgroundColor).toMatch(/#F97316/i)
          }
          return true
        }
      ),
      { numRuns: 100 }
    )
  })

  test('属性8：玻璃态卡片对比度', () => {
    // Feature: uniapp-ui-redesign, Property 8: 玻璃态卡片对比度
    fc.assert(
      fc.property(
        fc.hexaString({ minLength: 6, maxLength: 6 }).map(s => `#${s}`),
        (textColor) => {
          const bgColor = 'rgba(255, 255, 255, 0.8)'
          const contrast = calculateContrast(textColor, bgColor)
          return contrast >= 4.5
        }
      ),
      { numRuns: 100 }
    )
  })
})
```

**测试文件**：`tests/property/responsive.property.spec.ts`

```typescript
describe('响应式属性测试', () => {
  test('属性12：响应式布局断点', () => {
    // Feature: uniapp-ui-redesign, Property 12: 响应式布局断点
    fc.assert(
      fc.property(
        fc.integer({ min: 320, max: 1920 }),
        (width) => {
          const columns = getLayoutColumns(width)
          if (width < 768) {
            return columns === 1
          } else if (width >= 768 && width <= 1024) {
            return columns === 2
          } else {
            return columns >= 3
          }
        }
      ),
      { numRuns: 100 }
    )
  })
})
```

### 集成测试

集成测试验证组件间的交互和端到端流程。

#### 主题切换流程测试

```typescript
describe('主题切换集成测试', () => {
  test('应该完整执行主题切换流程', async () => {
    // 1. 初始化应用
    const app = createApp()
    const themeStore = useThemeStore()
    
    // 2. 加载主题
    await themeStore.getTheme()
    expect(themeStore.primaryColor).toBeTruthy()
    
    // 3. 切换主题色
    themeStore.setTheme('#10B981')
    expect(themeStore.primaryColor).toBe('#10B981')
    
    // 4. 验证CSS变量更新
    const root = document.documentElement
    const primaryColor = getComputedStyle(root).getPropertyValue('--color-primary')
    expect(primaryColor).toContain('#10B981')
  })
})
```

#### 购物车冲突检测测试

```typescript
describe('购物车冲突检测集成测试', () => {
  test('应该正确检测档期冲突', () => {
    const items: CartItem[] = [
      {
        id: 1,
        serviceDate: '2024-02-14',
        serviceTime: '14:00-16:00',
        // ...
      },
      {
        id: 2,
        serviceDate: '2024-02-14',
        serviceTime: '15:00-17:00', // 冲突
        // ...
      }
    ]
    
    const result = detectConflict(items)
    expect(result[0].hasConflict).toBe(true)
    expect(result[1].hasConflict).toBe(true)
  })
})
```

### 视觉回归测试

使用截图对比检测UI变化。

**工具**：Playwright + Percy

**测试场景**：
- 首页在不同主题色下的渲染
- 按钮在不同状态下的外观
- 卡片在hover状态下的效果
- 响应式布局在不同视口下的表现

### 无障碍测试

使用自动化工具检查无障碍性。

**工具**：axe-core

**检查项**：
- 颜色对比度
- 触摸目标尺寸
- 图片alt文本
- 表单标签
- ARIA属性

### 性能测试

**工具**：Lighthouse、WebPageTest

**指标**：
- First Contentful Paint (FCP) < 1.8s
- Largest Contentful Paint (LCP) < 2.5s
- Time to Interactive (TTI) < 3.8s
- Total Blocking Time (TBT) < 200ms
- Cumulative Layout Shift (CLS) < 0.1

**测试场景**：
- 首页加载性能
- 列表页滚动性能
- 图片懒加载效果
- 主题切换性能

