# Wedding Management 从零开发蓝图

## 1. 文档定位

本文是一个从零开发婚庆服务预约与管理平台的单一开发蓝图。后续 AI coding 或开发者只应依据本文设计新系统，不需要查看任何其他项目或历史实现。

本文的目标：

1. 明确目标系统应该具备哪些业务能力。
2. 明确第一版到完整运营版的开发阶段。
3. 明确核心领域模型、状态机和约束，防止 AI 自由发挥改坏业务逻辑。
4. 明确哪些能力可以暂缓，避免一开始把系统做重。

本文不是页面罗列，也不是甲方 PRD。它更接近一份可直接拆任务的产品 + 技术开发蓝图。

## 2. 目标系统概述

目标系统是一个婚庆服务预约与运营平台，覆盖展示获客、服务人员选择、档期预约、订单支付、履约协同、评价、问卷、售后、CRM 跟进、通知和财务结算。

系统由四类端组成：

| 端 | 定位 | 第一版边界 |
| --- | --- | --- |
| 移动端 / 小程序 | 用户交易和履约主端 | 浏览服务人员、查档期、下单、支付、看订单、接收通知 |
| PC 展示站 | 品牌展示和导流 | 首页展示、案例、联系顾问、小程序二维码 |
| Admin 后台 | 运营管理中心 | 用户、服务人员、套餐、档期、订单、支付配置、权限 |
| 后端服务 | 业务事实来源 | 认证、状态机、支付回调、锁档、通知、数据持久化 |

第一版重点是跑通“用户选择服务人员 -> 锁档 -> 创建订单 -> 后台确认 -> 支付 -> 订单进入待服务”的主链路。其他运营能力按阶段补齐。

## 3. 开发阶段

### M0：核心可运行骨架

目标：系统能启动，后台能登录，核心数据对象能创建。

范围：

- 后端基础框架。
- 数据库连接与迁移规范。
- 统一响应结构。
- 认证与权限中间件。
- Admin 登录。
- 管理员、角色、菜单、权限。
- 用户账号模型。
- 微信授权身份模型。
- 服务人员模型。
- 套餐和附加项模型。
- 档期模型。
- 订单模型。
- 支付流水模型。
- 基础配置、支付配置、存储配置。

不做：

- 不做复杂 UI。
- 不做问卷、售后、CRM、动态、报表、复杂装修。

### M1：预约支付闭环

目标：用户能完成一次标准预约支付。

范围：

- 小程序首页。
- 服务人员列表。
- 服务人员详情。
- 套餐和附加项选择。
- 档期查询。
- 服务端锁档。
- 锁过期处理。
- 订单预览。
- 创建订单。
- 后台确认订单。
- 支付方式。
- 预支付。
- 支付回调。
- 支付状态查询。
- 支付结果页。
- 订单列表和订单详情。
- 后台用户、服务人员、套餐、档期、订单、支付配置。

建议：

- M1 先支持全款支付。
- 测试环境提供后台模拟支付，方便不接真实微信支付也能验收主链路。
- 定金 + 尾款如业务必须，可放 M2；否则不要阻塞 M1。

### M2：履约闭环

目标：已支付订单能完成服务，并形成基础履约记录。

范围：

- 服务人员工作台。
- 服务人员订单列表和订单详情。
- 服务人员确认订单。
- 开始服务。
- 完成服务。
- 订单确认函生成、查看、历史版本和推送。
- 用户评价。
- 基础站内通知。
- 订单日志和履约状态记录。

### M3：运营增强

目标：系统可支撑真实试运营。

范围：

- 改期、暂停、加项。
- 退款申请、退款记录、异常支付补偿。
- 新人问卷：题库、草稿、发布版本、任务、发送、填写、答案回看。
- 售后工单、投诉、回访。
- CRM：客户、顾问、跟进记录、流失预警。
- 财务：资金流水、退款、成本、基础结算。
- 企业微信提醒、订阅消息、短信。
- PC 展示站更多区块和基础资讯。

### M4：完整运营能力

目标：补齐高级运营和非主链路能力。

范围：

- 动态社区。
- 复杂装修系统。
- PC 用户中心、收藏、资讯互动。
- 高级财务报表、对账、结算批次。
- 定时任务可视化。
- 复杂订阅消息统计。
- 素材中心高级能力。
- 多团队复杂结算。

## 4. AI 开发约束

以下规则必须作为每轮 AI coding 的硬约束。

1. 不允许前端自行推断关键业务状态。
2. 订单、支付、退款、档期状态必须以后端返回为准。
3. 所有金额计算必须在后端完成，前端只展示。
4. 创建订单必须使用服务端价格快照。
5. 支付流水必须独立于订单。
6. 支付回调必须幂等，不允许重复入账。
7. 不得把“收到支付成功回调”简化为直接修改订单状态。
8. 用户删除订单只做用户侧隐藏，不物理删除订单。
9. 后台删除核心业务数据默认改为禁用、关闭或软删除。
10. 档期锁必须服务端校验，不允许只靠前端倒计时。
11. 锁过期后旧锁不能继续提交订单。
12. 服务人员、套餐、作品下架不影响历史订单快照。
13. 问卷发布后版本不可变，已创建任务不受新版影响。
14. 已提交问卷不得被重复提交覆盖。
15. 权限必须同时控制菜单、按钮和后端接口，不允许只隐藏按钮。
16. 数据库迁移、字段命名、接口返回结构必须保持统一。
17. 核心状态流转必须有日志或可追踪记录。
18. PC 第一版只做展示导流，不做完整交易闭环。
19. 装修、动态、高级报表不得阻塞 M0-M1 主链路。
20. 遇到支付、退款、锁档、订单状态不确定时，必须先补状态机和测试用例。

## 5. 核心领域模型

### 5.1 账号体系

| 数据对象 | 说明 | 阶段 |
| --- | --- | --- |
| `users` | 消费者基础账号 | M0 |
| `user_auths` | 微信小程序、公众号、H5、App 等授权身份 | M0 |
| `admins` | 后台管理员账号 | M0 |
| `roles` | 后台角色 | M0 |
| `permissions` | 接口和按钮权限 | M0 |
| `menus` | 后台菜单 | M0 |
| `service_providers` | 服务人员业务档案，可关联 `user_id` | M0 |

规则：

- 消费者业务主键是 `user_id`。
- 后台管理员使用独立 `admin_id`。
- 服务人员是业务对象，不直接等同于用户账号；可通过 `user_id` 关联移动端身份。
- 微信 openid / unionid 存在 `user_auths`，不作为业务主键。
- 手机号建议在已验证维度唯一；手机号冲突进入人工处理或合并流程。

### 5.2 服务人员、套餐与作品

| 数据对象 | 说明 | 阶段 |
| --- | --- | --- |
| `service_providers` | 可预约服务人员 | M0 |
| `provider_teams` | 服务团队 | M3 |
| `provider_works` | 服务人员作品 | M1/M2 |
| `provider_certificates` | 证书 | M3 |
| `service_categories` | 服务分类 | M0 |
| `service_regions` | 服务地区 | M1 |
| `style_tags` | 风格标签 | M1 |
| `service_packages` | 服务套餐 | M0 |
| `service_addons` | 附加项 | M1 |
| `provider_package_prices` | 服务人员套餐和地区价格 | M1/M3 |

第一版建议：

- `service_providers` 作为可预约对象。
- 作品、套餐、档期先绑定 `service_provider_id`。
- 团队负责人权限后置。
- 结算对象先绑定服务人员，团队结算后置。

### 5.3 档期与候补

| 数据对象 | 说明 | 阶段 |
| --- | --- | --- |
| `provider_schedules` | 服务人员日期与时段可用配置 | M0 |
| `schedule_locks` | 临时锁档记录 | M0 |
| `schedule_occupations` | 正式占用记录 | M1 |
| `schedule_rules` | 全局或服务人员档期规则 | M2/M3 |
| `waitlists` | 候补记录 | M1/M3 |

规则：

- 使用 `date + time_slot` 建模。
- `time_slot` 可配置，默认支持全天、上午、下午、晚上或具体时段。
- 锁档 TTL 默认 15 分钟。
- 临时锁和正式占用可以分表，也可以用 `occupation_type` 区分，但语义必须清晰。
- 数据库必须对有效占用建立唯一约束，防止重复预约。

### 5.4 订单

| 数据对象 | 说明 | 阶段 |
| --- | --- | --- |
| `orders` | 订单主表 | M0 |
| `order_items` | 订单服务项 | M0 |
| `order_item_addons` | 订单附加项 | M1 |
| `order_logs` | 订单状态和操作日志 | M1 |
| `order_snapshots` | 价格、人员、套餐、档期快照，可内嵌或独立 | M1 |
| `order_confirm_letters` | 确认函版本 | M2 |
| `order_changes` | 改期、换人、加项等申请 | M3 |
| `order_pauses` | 暂停申请和暂停状态 | M3 |

第一版建议：

- 标准流程：待确认 -> 待支付 -> 待服务 -> 服务中 -> 已完成 -> 已评价。
- 待确认、待支付可取消，取消后释放可释放档期。
- 用户删除订单只做用户侧隐藏。
- M1 先支持全款支付。

### 5.5 支付、退款与财务

| 数据对象 | 说明 | 阶段 |
| --- | --- | --- |
| `payments` | 支付流水，独立于订单 | M0 |
| `payment_callbacks` | 回调原始记录或处理记录 | M1 |
| `refunds` | 退款申请和退款状态 | M3 |
| `fund_flows` | 资金流水 | M1/M3 |
| `recharge_orders` | 用户充值订单 | M3/M4 |
| `provider_settlements` | 服务人员结算 | M3/M4 |

第一版建议：

- 优先做微信支付 + 后台模拟支付。
- 支付流水必须有 `payment_sn`、`order_id`、`user_id`、`amount`、`status`、`transaction_id`。
- 真实微信支付上线前必须完成重复回调、金额不符、取消后回调测试。
- 余额、支付宝、线下凭证和混合支付后置。

### 5.6 问卷、售后与 CRM

| 数据对象 | 说明 | 阶段 |
| --- | --- | --- |
| `question_banks` | 问卷题库 | M3 |
| `questionnaires` | 服务人员问卷配置草稿 | M3 |
| `questionnaire_versions` | 不可变发布版本 | M3 |
| `questionnaire_tasks` | 订单问卷任务 | M3 |
| `questionnaire_answers` | 用户答案 | M3 |
| `after_sale_tickets` | 售后工单 | M3 |
| `complaints` | 投诉 | M3 |
| `service_callbacks` | 回访 | M3 |
| `crm_customers` | CRM 客户 | M3 |
| `crm_follow_records` | 跟进记录 | M3 |
| `sales_advisors` | 顾问 | M3 |
| `customer_loss_warnings` | 流失预警 | M3/M4 |

## 6. 核心状态机

### 6.1 订单状态转移表

| 当前状态 | 动作 | 下一状态 | 发起方 | 关键约束 |
| --- | --- | --- | --- | --- |
| 待确认 | 服务人员确认 / 管理员确认 | 待支付 | 服务人员 / 管理员 | 所有有效订单项确认后才推进整单。 |
| 待确认 | 用户取消 / 系统超时取消 | 已取消 | 用户 / 系统 | 释放未正式占用的档期。 |
| 待支付 | 支付成功回调 | 待服务 | 支付回调 | 回调必须校验金额、交易号、payer、订单状态。 |
| 待支付 | 用户取消 / 支付超时 | 已取消 | 用户 / 系统 | 释放可释放档期；取消后回调不得复活订单。 |
| 待服务 | 服务人员开始服务 | 服务中 | 服务人员 | 记录订单日志。 |
| 待服务 | 管理员取消 / 退款处理 | 已取消 / 退款中 | 管理员 | 已支付订单取消必须关联退款或异常处理。 |
| 服务中 | 服务完成 | 已完成 | 服务人员 / 用户 | 根据功能开关决定谁可完成。 |
| 已完成 | 用户评价 | 已评价 | 用户 | 一个订单评价应限制一次有效评价。 |
| 待服务 / 服务中 / 已完成 | 发起退款 | 退款中 | 用户 / 管理员 | 不直接改为已退款，等待退款结果。 |
| 退款中 | 退款成功 | 已退款 | 支付 / 管理员 | 写退款记录和资金流水。 |
| 退款中 | 退款失败 / 取消退款 | 原服务态 | 管理员 | 必须记录失败原因。 |
| 任意非终态 | 用户侧删除 | 状态不变 | 用户 | 只做用户侧隐藏。 |

### 6.2 支付状态转移表

| 当前状态 | 动作 | 下一状态 | 关键约束 |
| --- | --- | --- | --- |
| 待支付 | 创建预支付 | 待支付 | 生成唯一 `payment_sn`。 |
| 待支付 | 成功回调 | 已支付 | 金额、订单、交易号、payer 校验通过。 |
| 待支付 | 支付失败 | 支付失败 | 不推进订单。 |
| 已支付 | 重复成功回调 | 已支付 | 幂等返回，不重复入账。 |
| 已支付 | 发起退款 | 已支付 | 退款状态由 `refunds` 管理，不直接覆盖支付成功事实。 |
| 已支付 | 退款成功 | 已退款 / 部分退款 | 视退款金额决定，可用退款表表达部分退款。 |

支付回调拒绝场景：

- 金额不一致。
- `payment_sn` 不存在。
- 订单不处于可支付状态。
- `transaction_id` 已被其他支付流水使用。
- payer openid 不属于支付发起用户。
- 订单已取消或超时关闭。

### 6.3 档期状态转移表

| 当前状态 | 动作 | 下一状态 | 关键约束 |
| --- | --- | --- | --- |
| 可预约 | 用户锁档 | 已锁定 | 锁 TTL 默认 15 分钟；同一有效档期只能一个锁成功。 |
| 已锁定 | 锁过期 | 可预约 | 后端释放，前端倒计时只做展示。 |
| 已锁定 | 创建订单成功 | 已锁定 / 已预约 | 取决于业务是否先确认再正式占用。 |
| 已锁定 | 订单确认或支付成功 | 已预约 | 写正式占用记录。 |
| 已锁定 | 创建订单失败 | 可预约 | 事务失败必须释放本次锁。 |
| 已预约 | 订单取消且未支付 | 可预约 | 按订单状态和占用类型判断。 |
| 已预约 | 已支付订单改期 / 退款 | 可预约 / 不可用 | 必须走订单变更或退款流程。 |
| 可预约 | 后台预留 | 内部预留 | 不对用户开放。 |
| 内部预留 | 后台释放 | 可预约 | 记录操作日志。 |

档期锁规则：

- 谁能锁：已登录用户或明确的后台操作人。
- 锁多久：默认 15 分钟，可配置。
- 怎么释放：过期释放、订单取消释放、创建失败释放、后台解锁。
- 什么情况失败：已有有效锁、已有正式占用、服务人员下架、套餐下架、档期不可用、规则禁止。
- 并发怎么防：服务端 Redis / 数据库事务 + 数据库唯一有效占用约束。
- 支付超时怎么释放：订单变已取消后释放未正式履约占用。

### 6.4 问卷状态转移表

| 当前状态 | 动作 | 下一状态 | 关键约束 |
| --- | --- | --- | --- |
| 草稿 | 发布 | 已发布版本 | 发布版本不可变。 |
| 无任务 | 订单进入待服务 | 待提交任务 | 同一订单默认一个任务。 |
| 待提交 + 待推送 | 发送提醒 | 待提交 + 已推送 | 发送失败不阻塞订单。 |
| 待提交 | 用户查看 | 已查看 | 可选状态，记录 `viewed_time`。 |
| 待提交 / 已查看 | 用户提交 | 已提交 | 必填校验通过，答案唯一。 |
| 已提交 | 重复提交 | 已提交 | 幂等成功或明确提示，不覆盖答案。 |
| 待提交 / 已查看 | 管理员取消 | 已取消 | 记录取消原因。 |
| 待提交 / 已查看 | 过期 | 已过期 | 不可提交。 |

### 6.5 退款与售后状态

退款状态建议：

- 待审核。
- 退款中。
- 退款成功。
- 退款失败。
- 已取消。

售后工单状态建议：

- 待处理。
- 处理中。
- 待用户确认。
- 已完成。
- 已关闭。
- 已升级。

规则：

- 退款不直接等于订单取消，必须关联订单、支付流水、退款记录和资金流水。
- 售后处理不自动退款，涉及资金必须进入退款流程。

## 7. 端侧边界

### 7.1 移动端 / 小程序

移动端是交易和履约主端。

M1 必须做：

- 首页。
- 服务人员列表。
- 服务人员详情。
- 档期查询。
- 订单确认。
- 支付结果。
- 订单列表。
- 订单详情。
- 消息中心基础入口。

分阶段做：

- 新人问卷。
- 售后。
- 评价。
- 服务人员中心。
- 钱包。
- 动态社区。

### 7.2 PC 展示站

PC 第一版只做展示和导流。

M1 保留：

- PC 首页。
- 品牌介绍。
- 服务优势。
- 案例展示。
- 联系区。
- 小程序二维码或联系顾问 CTA。

M1 不做：

- PC 登录。
- PC 注册。
- PC 用户中心。
- PC 收藏。
- PC 下单、支付和订单详情。

### 7.3 Admin 后台

后台按阶段开放，不追求第一版做完全部菜单。

M0/M1 后台菜单：

- 登录。
- 管理员。
- 角色。
- 菜单 / 权限。
- 用户管理。
- 服务人员管理。
- 套餐管理。
- 档期管理。
- 订单管理。
- 支付配置。
- 基础系统配置。

M2 后台菜单：

- 服务人员订单处理。
- 订单确认函。
- 评价管理。
- 站内通知。

M3 后台菜单：

- 改期、暂停、加项。
- 退款管理。
- 新人问卷。
- 售后工单、投诉、回访。
- CRM 客户、顾问、跟进、流失预警。
- 财务流水、成本、结算。
- 企微、短信、订阅消息。

M4 后台菜单：

- 动态社区。
- 装修系统。
- 资讯内容。
- 高级报表。
- 定时任务可视化。

## 8. API 分层规则

| API 层 | 用途 | 示例 |
| --- | --- | --- |
| 用户端 API | 移动端 / 小程序调用 | 服务人员、档期、订单、支付、问卷、售后、评价、消息 |
| PC API | 展示站调用 | 首页配置、案例、联系信息、资讯 |
| 后台 API | Admin 调用 | 用户、服务人员、套餐、档期、订单、财务、CRM、系统设置 |
| 回调 API | 第三方服务回调 | 微信支付、支付宝、订阅消息、企微回调 |
| 内部任务 API / Job | 定时任务和补偿 | 支付超时、锁释放、流失预警、通知重试 |

统一规则：

- 统一响应结构：`code`、`msg`、`data`、`request_id`。
- 所有写操作必须鉴权。
- 核心写操作必须记录操作人和日志。
- 后台接口不能只依赖前端菜单隐藏。
- 支付回调接口不使用普通用户 token，但必须做签名和业务校验。
- 查询接口可以分页、筛选、排序，但不能暴露越权数据。

## 9. 数据库设计原则

1. 表名按领域命名，不按页面命名。
2. 核心表必须有 `id`、`create_time`、`update_time`。
3. 核心业务表优先软删除或禁用，不物理删除。
4. 金额字段使用 decimal，不使用 float。
5. 订单必须保存价格快照和服务快照。
6. 支付流水必须保存第三方交易号和回调处理状态。
7. 档期有效占用必须有唯一约束。
8. 问卷任务必须保存版本号和题目快照。
9. 所有关联订单的售后、问卷、评价、退款都必须保留 `order_id` 和 `user_id`。
10. CRM 客户应支持绑定 `user_id` 和手机号。
11. 重要状态变更必须有日志表或审计字段。
12. 数据库迁移必须可回滚或有明确补偿方案。

## 10. 模块开发顺序

建议按以下顺序拆任务：

1. 基础工程、认证、统一响应、数据库迁移规范。
2. Admin 登录、角色、权限、菜单。
3. 用户、微信授权、手机号绑定。
4. 服务人员、套餐、附加项。
5. 档期、锁档、正式占用。
6. 订单、订单项、订单日志、订单快照。
7. 支付流水、预支付、支付回调、模拟支付。
8. 移动端首页、服务人员列表、详情、档期、订单确认、支付结果、订单详情。
9. 后台订单确认和服务人员订单处理。
10. 确认函、通知、评价。
11. 退款、改期、暂停、加项。
12. 问卷、售后、CRM、财务。
13. PC 展示、装修、动态、报表和高级运营能力。

## 11. 验收与回归用例

### M0 验收

- 后端可启动，数据库迁移可执行。
- Admin 可登录。
- 管理员、角色、菜单、接口权限可配置。
- 用户、服务人员、套餐、档期、订单、支付流水基础表存在。
- API 返回结构统一，并带 `request_id`。

### M1 验收

- 用户可登录并浏览服务人员。
- 用户可选择套餐和档期。
- 同一服务人员同一日期时段并发锁档只成功一次。
- 锁过期后不能提交订单。
- 用户可创建订单。
- 后台可确认订单。
- 用户可支付订单。
- 支付回调重复通知不重复入账。
- 支付成功后订单进入待服务。
- 订单详情展示当前状态和可用动作。

### M2 验收

- 服务人员可查看并处理订单。
- 订单可进入服务中和已完成。
- 确认函可生成、查看和保留历史版本。
- 用户可评价已完成订单。
- 站内通知可跳转到订单或确认函。

### M3 验收

- 用户可申请改期、暂停、加项和退款。
- 后台可处理申请并记录原因。
- 新人问卷支持发布版本、任务、发送、填写和答案回看。
- 问卷重复提交不覆盖答案。
- 售后工单可创建、分配、处理、关闭。
- CRM 能按 `user_id` 或手机号关联客户、订单、问卷、售后和评价。
- 财务能追踪支付、退款和资金流水。

### 支付专项回归

- 金额不一致回调不推进订单。
- 订单号不匹配回调不推进订单。
- 第三方交易号复用被拒绝。
- payer 不一致被拒绝。
- 已取消订单收到成功回调不复活订单。
- 重复成功回调幂等。

### 档期专项回归

- 两个用户同时锁同一档期，只有一个成功。
- 锁过期自动释放。
- 创建订单失败释放锁。
- 待支付订单取消释放可释放档期。
- 已支付订单改期必须走订单变更流程。

## 12. 暂缓开发功能

以下功能不是废弃，而是不应阻塞 M0-M1 预约支付主链路：

- PC 登录、注册、用户中心、收藏。
- 动态社区完整互动。
- 复杂拖拽装修。
- 高级财务报表。
- 定时任务可视化。
- 复杂订阅消息统计。
- 多团队复杂结算。
- 混合支付。
- 支付宝和余额支付。
- 线下凭证完整审核流程。
- 素材中心高级能力。

## 13. 目标功能总览

| 领域 | 目标能力 | 推荐阶段 |
| --- | --- | --- |
| 账号 | 登录、注册、微信授权、手机号、资料、密码、钱包 | M0-M3 |
| 展示 | 首页、品牌、案例、PC 联系导流、资讯 | M1/M4 |
| 服务人员 | 列表、详情、作品、证书、团队、套餐、附加项、收藏 | M1-M3 |
| 档期 | 查询、锁定、释放、预留、规则、候补、日历事件 | M1-M3 |
| 订单 | 预览、创建、确认、支付、详情、日志、确认函 | M1-M2 |
| 支付 | 支付方式、预支付、回调、状态查询、异常补偿 | M1-M3 |
| 退款变更 | 退款、改期、暂停、加项 | M3 |
| 问卷 | 题库、草稿、发布版本、任务、发送、填写、答案 | M3 |
| 售后 | 工单、投诉、回访、升级问题 | M3 |
| 评价 | 评价、审核、回复、标签、敏感词、分享奖励 | M2-M4 |
| CRM | 客户、顾问、跟进、流失预警、企微提醒 | M3-M4 |
| 财务 | 流水、充值、退款、成本、结算、报表、对账 | M3-M4 |
| 通知 | 站内信、订阅、短信、企微、通知日志 | M2-M4 |
| 后台治理 | 权限、菜单、组织、系统设置、支付设置、存储、定时任务 | M0-M4 |

## 14. 最终判断

如果目标是从零开发一个能真实运营的新系统，正确顺序是：

1. 先搭骨架。
2. 再跑通预约支付。
3. 再补履约。
4. 再补运营。
5. 最后补高级功能。

每一阶段都应有可运行系统、可验证接口和可复现测试，而不是只完成静态页面。
