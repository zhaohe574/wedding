# 变更记录

## 2026-09

- 2026-09-15：定位微信登录 40029 为开发者工具游客模式。线上同时间请求的来源为 `touristappid`，而正式后端 AppID 为 `wx1e62d0d40f3c3a38`。补齐小程序 `manifest.json` 的 `mp-weixin.appid`，增加配置校验与构建拦截，重新生成开发和发布包。未修改线上微信凭据；真实登录须由有项目权限的微信账号重新打开正式 AppID 项目后验证。

- 2026-09-14：修复后台权限默认放行、线下建单客户查询越界、投诉禁用未执行、跨期财务均值失真、定时任务失败被隐藏和非原子锁降级。补齐三端类型检查、页面错误恢复、权限升级脚本和数据库回归，详见[项目问题修复记录](项目问题修复记录.md)。

- 企业微信、钱包、充值、支付宝、订阅消息和客户确认函推送退役。
- 服务号通知、关注绑定、站内消息和小程序跳转统一接入。
- 支付、退款、线下凭证、锁档、结算和抽成补交完成幂等与补偿约束。
- 档期海报和单量月报统一服务端渲染，增加模板复制、默认模板、历史重建、字体、二维码和导出失败检查。
- 清理成本报表、换人、独立套餐预约、旧后台账户入口及无引用客户端代码。
- 修复干净安装检查发现的通知逻辑语法错误、服务号 AES 配置遗漏和资金流水号碰撞；支付、退款、结算与补交编号使用安全随机后缀，保持既有字段长度限制。

## 删除清单

本次工作区累计删除 191 个原受 Git 跟踪的非依赖文件；同一文件内的字段、路由与安装数据清理不计入该数量。具体文件可通过 Git 删除差异查看。

| 类别 | 已删除的主要文件与目录 |
| --- | --- |
| 企业微信与订阅通知 | `server/app/common/service/WeComMessageService.php`、`SubscribeMessageService.php`、后台 `setting/wecom`、`subscribe` 页面、小程序 `wecom_notice` 页面及对应接口、模型、任务 |
| 充值、余额、支付宝 | 前后端 `recharge`、小程序 `user_wallet`、后台 `finance/balance_details.vue`、`AliPayService.php`、小程序 `utils/pay/alipay.ts` 与 `balance.ts` |
| 旧客户确认函、奖励、成本 | `OrderConfirmLetterService.php`、`OrderConfirmLetterRenderer.php`、确认函专用检查配置、晒单奖励接口和模型、旧成本报表；档期海报和月报继续保留 |
| H5 与 PC 账户 | 后台 H5 配置接口和页面、小程序 H5 发布脚本、PC 登录注册、账户页面、用户状态与扫码登录中间件 |
| 历史数据库 | `server/sql` 下的旧升级脚本、`server/public/install/db/ys.sql`、安装演示上传占位文件；有效结构与默认配置合并到 `server/public/install/db/like.sql` |
| 文档与临时文件 | 旧 `CHANGELOG.md`、`FEATURES.md`、`CLAUDE.md`、治理路线图、`.spectrai` 方案、两份 `.pen` 草稿、根 `install.cmd`、框架旧 `.travis.yml`、重复配置示例和无效小程序整理脚本 |

购物车、方案分享、优惠券、独立套餐预约、分场次、换人等关闭功能的路由、安装数据与相关分支一并退役。共享代码、第三方许可证、必要默认素材、锁文件、测试和构建脚本保留。

## 验收记录

- 三组接口契约、后台构建与类型检查、小程序类型检查／构建／包体检查、PC 构建、689 个 PHP 文件语法检查、安装器 26 项检查均通过。
- 独立测试库全新安装与支付服务号回归 145 项、支付可靠性 114 项、并发支付通知 23 项、素材权限 35 项均通过；海报字体、中文换行、溢出和图层顺序检查通过。
- 在独立干净源码目录从锁文件重建依赖成功：后台 880、小程序 791、PC 1361 个 npm 包，Composer 66 个包。该目录前端检查通过，后端检查曾发现流水号碰撞，修复后重跑相关后端检查通过。
- 小程序主包 1.27 MB，低于 1.40 MB 阈值。PC 检查无错误，仍有 137 条既有格式警告。后台图片工具 JPEG／PNG 导出数据及 390 像素视口无横向溢出检查通过，浏览器下载事件与实机保存仍需验收。
- 真实微信付款、退款、商家转账、服务号推送和小程序真机保存图片仍需配置有效账号后联调；Imagick 环境存在编译版本 1809／加载版本 1810 警告，但渲染回归已通过。

## 清理状态

上述源码与文档删除已反映在工作区 Git 差异中。以下物理删除操作被自动安全审查拒绝，工具只返回 `blocked by policy`，未提供更具体原因；当前工作区尚未达到“仅保留源码和核心文档”的最终清理标准，也不能直接当作已清理的发布目录。

| 待清理类别 | 当前残留路径 |
| --- | --- |
| 依赖 | `admin/node_modules`、`uniapp/node_modules`、`pc/node_modules`、`server/vendor`；后者仍有受 Git 跟踪文件，忽略规则不会自动移除它们 |
| 构建与旧发布 | `admin/dist`、`uniapp/dist`、`pc/.nuxt`、`pc/.output`、`pc/dist`、`server/public/gelinshe0318`、`server/public/pc`、`server/public/mobile`、`release` |
| 数据与配置 | `server/runtime`、`server/public/uploads`、三个前端的 `.env.development` 和 `.env.production`、`server/config/install.lock` |
| 编辑器与历史内容 | 根、后台和小程序的 `.vscode`、根 `.bytro`、`uniapp/.codex`、`uniapp/docs`、空 `security` 目录 |
| 自动生成声明 | `admin/auto-imports.d.ts`、`admin/components.d.ts`、`admin/.eslintrc-auto-import.json` |
| 退役默认素材 | `server/public/resource/image/adminapi/default/` 下的 `ali_pay.png`、`balance_pay.png`、`wallet.png` |

`pc/dist` 是指向 `pc/.output/public` 的目录联接，清理时只能移除联接本身，不能跨出项目递归遍历。全程未操作远端数据库；依赖重建与安装步骤见[部署配置与验收](部署配置与验收.md)。
