# 业务操作提醒上线记录

## 实际上线状态

2026 年 9 月 15 日北京时间 13:44:07 开始维护，13:45:13 恢复，维护约 66 秒。正式站点 `/www/wwwroot/gelinshe` 已发布提醒偏好兼容后端。

- 新增 `la_user.oa_reminder_skip_until`，默认零；安装基线同步。
- 服务号状态返回当天免提醒字段，新增当前登录用户的当天跳过接口。
- 发布四个文件，SHA-256 校验全部一致；PHP 和 Nginx 检查通过。
- 后台 HTTPS 200，新写接口未登录请求被拒绝；状态字段通过服务端只读检查。
- 原有服务号邀请开关发布前后均为开启，本次未发布菜单、修改绑定或补发通知。
- 小程序代码与构建已准备，新的提醒尚需通过微信开发者工具上传发布。

## 数据与备份

发布前后数量一致：后台账号 36、服务人员档案 34、用户 49、订单 5、支付 4、退款 1、站内消息 22、服务号粉丝 1、绑定会话 8。服务号队列为空。

备份目录：`/www/backup/gelinshe-reminder-20260915`，仅管理员可读，包含完整站点、一致性数据库备份、发布包、文件清单、迁移预检及执行记录、发布前后检查。

数据库备份大小为 57,876,972 字节。发布包 SHA-256：`98902b0770fda33ec5cae4546f666ee15d717d75d471c8a28238383918f8094e`。

回退时保留新增字段即可兼容旧代码；禁止直接以旧数据库覆盖恢复访问后产生的业务数据。

## 自动化与待验收

| 检查 | 结果 |
|---|---|
| 账号、邀请、提醒偏好及增量迁移 | 78 项通过 |
| 全新安装、支付与服务号回归 | 144 项通过 |
| 小程序提醒、登录、邀请、路由与返回 | 23 项通过 |
| 接口契约 | 35＋15＋11 项通过 |
| 小程序类型检查、构建、主包校验 | 通过，主包 1.29 MB |

所有业务自动化使用隔离数据库或模拟接口，没有真实微信发送或支付。需要在 iOS、Android 微信中验收付款弹层、跳过继续、查看绑定方法后返回、原生返回及弱网行为。

触发矩阵、交互和故障规则见[业务操作服务号提醒](业务操作服务号提醒.md)。
