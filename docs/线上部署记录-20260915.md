# 账号绑定与服务号通知上线记录

## 上线结果

2026 年 9 月 15 日已将后端与管理后台部署到正式机 `42.193.105.236`，站点目录为 `/www/wwwroot/gelinshe`。北京时间 11:16:19 开始维护，11:17:55 恢复访问，维护约 1 分 36 秒。

正式后台：[https://www.gelinshe.com/gelinshe0318/](https://www.gelinshe.com/gelinshe0318/)。11:18 自动调度成功，退出码为 0；启用任务的错误字段均为空，服务号派发任务执行时间已推进。

## 实际部署与迁移

- 部署后端、增量迁移、安装基线及管理后台构建资源，共 1,231 个文件，正式文件与本地 SHA-256 清单逐一匹配。
- 新增绑定审计、业务通知事件、消息身份与业务目标、首次关注引导字段，以及服务人员当前身份的唯一约束。
- 补齐 27 条已有明确人员档案对应的后台用户关联，并生成 27 条迁移审计。不按手机号或姓名推断归属。
- 首次预检发现 5 组重复关联，核对后均为已删除历史档案与当前档案重复。改用生成列，仅对未删除档案建立唯一约束，历史记录及原关联完整保留；当前有效档案不存在冲突。
- 生成列迁移先在正式机 MySQL 5.7 的临时表验证，通过后才修改正式表；临时表验证没有改动正式业务表。
- 登记新增后台操作权限，未自动扩大岗位授权。账号绑定管理权限由超级管理员按岗位分配。
- 保留正式配置、密码盐、上传文件、运行证书、依赖及 PC 页面；保留后台旧哈希资源以兼容已打开页面。发布前后 Composer 锁文件一致。
- 无历史服务号队列待发送；没有补发历史提醒，也未主动发送真实测试通知。

## 验证记录

| 检查 | 结果 |
|---|---|
| 账号、服务号及增量迁移回归 | 42 项通过，包含软删除历史关联保留 |
| 并发支付、账号竞争及通知队列 | 26 项通过 |
| 全新安装、支付退款及模拟服务号 | 144 项通过 |
| 正式机 PHP 语法与 Nginx 配置 | 通过 |
| 发布文件校验 | 1,231 个全部匹配 |
| 当前服务人员关系不一致数 | 0 |
| 后台首页、嵌套路由、PC 页面 | HTTP 200 |
| 后台公开配置、小程序全局配置 | HTTP 200，业务成功 |
| 未登录查询服务号状态、账号绑定详情 | 拒绝，返回缺少登录凭据 |
| 通知派发空队列检查 | 退出码 0，处理 0，失败 0 |
| 自动调度恢复 | 11:18 执行成功，启用任务无错误 |
| 独立外部访问 | 本机直连正式 IP 并验证域名 TLS，后台 HTTP 200 |

迁移前后，后台账号 36、人员档案 34、平台用户 49、订单 5、支付记录 4、退款记录 1、历史站内消息 22、后台角色关系 28、角色菜单授权 386，数量均一致。

## 备份与回退

备份目录为 `/www/backup/gelinshe-20260915-1110`，目录和文件仅管理员可读。

| 文件 | 内容 |
|---|---|
| `site-before.tar.gz` | 维护期间暂停 PHP 请求后的完整站点备份，约 350 MB |
| `database.sql` | 暂停请求及任务后的数据库一致性备份，约 55 MB |
| `release.tar.gz`、`release-manifest.json` | 本次正式发布包及逐文件校验清单 |
| `relationship-report.txt` | 迁移前只读关系与结构计划 |
| `migration-applied.log` | 正式迁移执行记录 |
| `before-check.json`、`after-check.json`、`final-check.json` | 数据数量与调度复核记录 |

发布包 SHA-256：`ec7a8689bf2e9f105fc7c97f18824bd1fcb7ff7e2a951d236d78e05a74f79a35`。

维护标记仍为 `/www/maintenance/gelinshe.flag`，本次已移除。回退前必须重新暂停请求及调度，并备份恢复服务后的新增数据；不得直接用本次旧数据库覆盖新订单或支付。恢复时需使用匹配的代码、数据库及权限配置。

## 尚未发布的部分

小程序构建已通过，但 SSH 部署不会发布微信小程序。新的关注引导、确认页面与通知入口仍需通过微信开发者工具上传、审核和发布；iOS、Android 微信真机关注及真实模板送达仍需指定测试账号联调。

账号关系、页面流程、通知矩阵详见[账号绑定与服务号通知](账号绑定与服务号通知.md)。

## 服务号配置页补充修复

11:28:37 修复并上线服务号配置读取错误。正式环境复现表明，多应用 URL 组件将当前框架返回的路由绑定字符串当作数组遍历，生成回调地址时抛出 `foreach()` 类型错误。

服务号回调改为当前请求域名加固定回调路径，不经过不兼容的反向路由逻辑；配置域名由请求对象读取。修复前后的正式环境逻辑调用已验证：旧代码稳定报错，新代码成功返回配置字段及正确 HTTPS 回调地址。PHP 语法检查通过，后台 HTTP 200，线上文件与本地 SHA-256 一致。没有修改服务号配置值或数据库结构。

本次单文件原子替换并平滑重载 PHP-FPM。原文件备份于 `/www/backup/gelinshe-oa-config-hotfix-20260915/OfficialAccountSettingLogic.before.php`。该文件的最终 SHA-256 为 `93c620c58e52f35141be6e777022a9b2eaf6b2c54d28f617823dfbc99e8bc11c`，覆盖首次发布清单中该文件的旧值。
