# 管理后台

Vue 3、Vite、Element Plus。负责 CRM、订单、财务、内容、海报与图片工具。

按[部署说明](../docs/部署配置与验收.md)配置 `.env` 后执行 `npm ci`。开发使用 `npm run dev`，类型检查使用 `npm run type-check:active`，发布使用 `npm run build`，输出复制至 `server/public/gelinshe0318`。
